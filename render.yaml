services:
  - type: web
    name: defund-backend
    env: node
    region: oregon
    rootDir: defund/backend
    buildCommand: npm install && npx tsc
    startCommand: node dist/index.js
    envVars:
      - key: PORT
        value: 3001
      - key: NODE_ENV
        value: production
